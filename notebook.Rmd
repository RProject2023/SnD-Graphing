---
title: "R Notebook"
output: html_notebook
---

```{r}
source("supply-demand-linear.R")

```

p = (x1, y1, x2, y2)

```{r}
p1=c(2, 3, 5, 7)

linear_curve_2_ponts(p1, title="Supply")
linear_curve_2_ponts(p1, title="Supply", color = "green", pointsHidden=FALSE, lineCoordinateHidden=FALSE)

```

```{r}
p2 = c(3, 10, 6, 7)
linear_curve_2_ponts(p2, title = "Demand")
linear_curve_2_ponts(p2, title = "Demand", color = "red", pointsHidden=FALSE, lineCoordinateHidden=FALSE)
```

```{r}
p3 = c(3, 2, 7, 7)

linear_curve_2_ponts(p1, p2, title="Supply & Demand", color = "blue")
gg <- linear_curve_2_ponts(p1, p2, p3, title="Supply & Demand", color = "blue")
gg <- find_intersection(p1, p2, gg)
find_intersection(p2, p3, gg)
```

```{r}
linear_curve_2_characteristic(c(2, -5), c(-2, 10), title="Supply & Demand", color = "blue")
linear_curve_2_characteristic(c(2, -5), c(-2, 10), c(-2, 13), title="Supply & Demand", color = "blue")
```

```{r}
p1 <- c(1, 2, 5, 7)
p2 <- c(3, 9, 6, 4)

gg <- linear_curve_2_ponts(p1, p2, title="Supply & Demand", color = "blue")
find_intersection(p1, p2, gg)
```

```{r}
library(dplyr)
library(ggplot2)
library(Hmisc)

supply <- bezier(x = c(1, 8, 9), y = c(1, 5, 9)) %>% as.data.frame()
demand <- bezier(x = c(1, 3, 9), y = c(9, 3, 1)) %>% as.data.frame()

curve_N_ponts(supply, demand, title = "Supply & Demand",linecol = c("blue", "red"))
```

```{r}
demand2 <- bezier(x = c(3, 5, 11), y = c(9, 3, 1)) %>% as.data.frame()

gg <- curve_N_ponts(supply, demand, demand2, title = "Supply & Demand",linecol = c("blue", "red", "pink"))

gg <- curve_intersect(supply, demand, gg)
curve_intersect(supply, demand2, gg)
```
